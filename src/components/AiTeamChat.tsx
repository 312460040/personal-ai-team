import React, { useEffect, useRef, useState } from 'react';
import ReactMarkdown from 'react-markdown';
import { Send, Bot, User, Briefcase, GraduationCap, Sparkles, Copy, Check, ShieldCheck, AlertTriangle } from 'lucide-react';
import confetti from 'canvas-confetti';
import { ChatMessage, AgentInfo, StructuredTimeBlock, WorkTask, StudyTask, WorkProject, StudySubject, AgentActivityLog, AgentId, AgentExecutionAudit } from '../types';
import { AgentActivityPipeline } from './AgentActivityPipeline';
import { ChatTaskChecklist } from './ChatTaskChecklist';
import { apiUrl } from '../services/apiBase';
interface AiTeamChatProps { messages: ChatMessage[]; onSendMessage: (text: string) => void; isLoading: boolean; onApplyScheduleToToday: (blocks: StructuredTimeBlock[]) => void; currentActiveAgents: string[]; allowedAgentIds?: string[]; agentRegistry?: AgentInfo[]; workTasks?: WorkTask[]; studyTasks?: StudyTask[]; workProjects?: WorkProject[]; studySubjects?: StudySubject[]; onToggleWorkTask?: (id: string) => void; onToggleStudyTask?: (id: string) => void; onUpdateWorkTask?: (task: WorkTask) => void; onUpdateStudyTask?: (task: StudyTask) => void; chatRoomId?: string; }
type DirectAgent = { id: string; name: string; role: string; icon: React.ReactNode };
const defaultAgents: DirectAgent[] = [{ id: 'manager', name: 'Manager', role: 'AI 總管', icon: <Bot className="w-3.5 h-3.5" /> }, { id: 'work', name: 'Work Agent', role: '工作管理員', icon: <Briefcase className="w-3.5 h-3.5" /> }, { id: 'study', name: 'Study Agent', role: '課業管理員', icon: <GraduationCap className="w-3.5 h-3.5" /> }];
const agentLabel = (id: string) => id === 'work' ? 'Work Agent' : id === 'study' ? 'Study Agent' : 'Manager Agent';
const ROOM_STORAGE_KEY = 'ait_agent_chat_rooms_v2';
const loadRooms = (): Record<string, ChatMessage[]> => { try { const raw = localStorage.getItem(ROOM_STORAGE_KEY); return raw ? JSON.parse(raw) : {}; } catch { return {}; } };
const auditSummary = (audit?: AgentExecutionAudit) => { if (!audit) return ''; const mode = audit.executionMode === 'parallel_specialists_then_manager' ? 'Work + Study → Manager' : audit.finalAgent === 'manager' ? 'Manager' : agentLabel(audit.finalAgent); return `${mode}｜寫入權限 ${audit.writeAuthorized ? '允許' : '未授權'}｜AI 提案 ${audit.requested}｜接受 ${audit.accepted}｜攔截 ${audit.rejected}`; };
export const AiTeamChat: React.FC<AiTeamChatProps> = ({ messages, onSendMessage, isLoading, onApplyScheduleToToday, currentActiveAgents, allowedAgentIds, agentRegistry = [], workTasks = [], studyTasks = [], workProjects = [], studySubjects = [], onToggleWorkTask = () => {}, onToggleStudyTask = () => {}, onUpdateWorkTask = () => {}, onUpdateStudyTask = () => {}, chatRoomId = 'room-public' }) => {
 const [inputText,setInputText]=useState(''); const [directAgentId,setDirectAgentId]=useState('manager'); const [roomMessages,setRoomMessages]=useState<Record<string,ChatMessage[]>>(loadRooms); const [directLoading,setDirectLoading]=useState(false); const [selectedTaskIds,setSelectedTaskIds]=useState<string[]>([]); const [expandedActivities,setExpandedActivities]=useState<Record<string,boolean>>({}); const [copiedId,setCopiedId]=useState<string|null>(null); const messagesEndRef=useRef<HTMLDivElement>(null);
 const allowed=allowedAgentIds||currentActiveAgents; const availableAgents=defaultAgents.filter(a=>allowed.includes(a.id)); const registryAgents=agentRegistry.filter(a=>allowed.includes(a.id)&&!defaultAgents.some(x=>x.id===a.id)); const selectableAgents=[...availableAgents,...registryAgents.map(a=>({id:a.id,name:a.name,role:a.roleName,icon:<Bot className="w-3.5 h-3.5"/>}))]; const selectedAgent=selectableAgents.find(a=>a.id===directAgentId)||selectableAgents[0]||defaultAgents[0]; const roomKey=chatRoomId; const displayMessages=[...messages,...(roomMessages[roomKey]||[])]; const busy=directLoading||isLoading;
 useEffect(()=>{if(!selectableAgents.some(a=>a.id===directAgentId))setDirectAgentId(selectableAgents[0]?.id||'manager');},[allowedAgentIds?.join(','),currentActiveAgents.join(','),agentRegistry.length]); useEffect(()=>{messagesEndRef.current?.scrollIntoView({behavior:'smooth'});},[displayMessages,busy]);
 const appendRoomMessages=(key:string, updater:(current:ChatMessage[])=>ChatMessage[])=>{setRoomMessages(prev=>{const next={...prev,[key]:updater(prev[key]||[])}; try{localStorage.setItem(ROOM_STORAGE_KEY,JSON.stringify(next));}catch{} return next;});};
 const applyActions=(actions:any[])=>{const ids:string[]=[]; actions.forEach(action=>{const domain=action.domain==='study'?'study':'work'; const source=domain==='study'?studyTasks.find(t=>t.id===action.taskId):workTasks.find(t=>t.id===action.taskId); if(!source)return; const next:any={...source,...(action.updates||{}),source:source.source,createdBy:source.createdBy}; domain==='study'?onUpdateStudyTask(next as StudyTask):onUpdateWorkTask(next as WorkTask); ids.push(String(action.taskId));}); return ids;};
 const verifyLocalPersistence=(key:string,messageId:string,taskIds:string[],actions:any[])=>{window.setTimeout(()=>{try{const work:WorkTask[]=JSON.parse(localStorage.getItem('ait_work_tasks_v2')||'[]'); const study:StudyTask[]=JSON.parse(localStorage.getItem('ait_study_tasks_v2')||'[]'); const all:any[]=[...work,...study]; const verified=taskIds.length>0&&taskIds.every(id=>{const saved=all.find(t=>t.id===id); const action=actions.find(a=>String(a.taskId)===id); return !!saved&&!!action&&Object.entries(action.updates||{}).every(([key,value])=>JSON.stringify((saved as any)[key])===JSON.stringify(value));}); appendRoomMessages(key,prev=>prev.map(m=>{if(m.id!==messageId)return m; const logs=[...(m.activityLogs||[])]; const i=logs.findIndex(l=>l.action==='套用 Task 更新'); if(i>=0)logs[i]={...logs[i],status:verified?'completed':'warning',summary:verified?`已驗證 ${taskIds.length} 筆 Task 已寫入本機共享資料狀態。`:'AI 已提出更新，但目前無法驗證持久化結果，未宣稱完成。',detail:verified?'驗證來源：localStorage（AppDataContext 的持久化狀態）。':'請重新整理頁面確認；系統刻意不把未驗證結果視為完成。'}; return {...m,executionVerified:verified,activityLogs:logs};}));}catch(e){console.error('Agent execution verification failed:',e);}},80);};
 const buildActivityLogs=(data:any,prompt:string,appliedIds:string[]):AgentActivityLog[]=>{const r=data?.routing;if(!r)return[]; const rawSteps=Array.isArray(data?.executionPlan?.steps)?data.executionPlan.steps:[]; const mode=data?.execution?.mode||(r.intent==='mixed'?'parallel_specialists_then_manager':'single_agent'); const logs:AgentActivityLog[]=[]; let index=1; const add=(fromAgent:AgentId,action:string,summary:string,detail?:string,toAgent?:AgentId,status:AgentActivityLog['status']='completed')=>{logs.push({id:`act-${Date.now()}-${index}`,timestamp:new Date().toISOString(),stepIndex:index++,fromAgent,toAgent,action,summary,detail,status,durationMs:0});};
 add('manager','Manager 分析需求並分流',r.reason||'Manager 已完成需求判斷。',`需求：「${prompt}」｜${r.requiresDataWrite?'可能需要資料寫入':'先進行唯讀分析或對話'}`,'manager');
 if(mode==='parallel_specialists_then_manager'||r.intent==='mixed'){const specialists=Array.isArray(r.delegatedAgents)?r.delegatedAgents.filter((id:string)=>id==='work'||id==='study'):['work','study']; specialists.forEach((id:AgentId)=>{const step=rawSteps.find((s:any)=>s.agentId===id); add(id,`${agentLabel(id)} 執行`,step?.purpose||(id==='work'?'讀取工作 User Data 並處理工作需求。':'讀取課業／研究 User Data 並處理學習需求。'),id==='work'?'Work Agent 已完成工作領域分析。':'Study Agent 已完成課業／研究領域分析。','manager');}); add('manager','Manager 整合專業 Agent 結果',data.execution?.managerSynthesis||'Manager 已接收 Work Agent 與 Study Agent 結果並形成最終回覆。');} else {const effective=String(r.effectiveAgentId||data.agentId||'manager') as AgentId; if(effective!=='manager'){const step=rawSteps.find((s:any)=>s.agentId===effective); add(effective,`${agentLabel(effective)} 執行`,step?.purpose||'專業 Agent 讀取對應 User Data 並處理需求。',Array.isArray(data.actions)&&data.actions.length?`已提出 ${data.actions.length} 項資料更新。`:'本次未產生資料更新。','manager'); add('manager','Manager 接收專業 Agent 結果','專業 Agent 已完成本輪處理，結果交回 Manager。');}}
 if(Array.isArray(data.actions)&&data.actions.length){add((r.effectiveAgentId||data.agentId||'manager') as AgentId,'套用 Task 更新',appliedIds.length?`已套用 ${appliedIds.length} 筆 Task 更新，正在驗證持久化結果。`:'AI 提出更新，但找不到可安全修改的 User Task。',appliedIds.length?appliedIds.join('、'):'未執行寫入。',undefined,appliedIds.length?'running':'warning');} return logs;};
 const handleDirectSend=async(text:string)=>{const prompt=text.trim();if(!prompt||directLoading)return; const key=roomKey; const roomHistory=roomMessages[key]||[]; const userMessage:ChatMessage={id:`direct-user-${Date.now()}`,sender:'user',text:prompt,timestamp:new Date().toLocaleTimeString('zh-TW',{hour:'2-digit',minute:'2-digit',hour12:false})}; const history=[...messages,...roomHistory,userMessage]; appendRoomMessages(key,prev=>[...prev,userMessage]); setDirectLoading(true); try{const response=await fetch(apiUrl('/api/agent/direct/chat'),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:prompt,agentId:selectedAgent.id,agentName:selectedAgent.name,agentRole:selectedAgent.role,history,context:{workProjects,workTasks,studySubjects,studyTasks,selectedTaskIds,currentContext:{workspaceId:selectedAgent.id==='study'?'study':selectedAgent.id==='work'?'work':'manager',chatRoomId}}})}); if(!response.ok)throw new Error(`Direct agent chat returned ${response.status}`); const data=await response.json(); const appliedIds=Array.isArray(data.actions)?applyActions(data.actions):[]; const activityLogs=buildActivityLogs(data,prompt,appliedIds); const messageId=`direct-agent-${Date.now()}`; if(appliedIds.length){setSelectedTaskIds(p=>p.filter(id=>!appliedIds.includes(id)));confetti({particleCount:30,spread:55,origin:{y:.85}});} appendRoomMessages(key,prev=>[...prev,{id:messageId,sender:'agent',agentId:data.agentId||selectedAgent.id,agentName:data.agentName||selectedAgent.name,agentRole:data.agentRole||selectedAgent.role,text:data.text||'我有收到，你可以繼續說。',timestamp:new Date().toLocaleTimeString('zh-TW',{hour:'2-digit',minute:'2-digit',hour12:false}),delegatedAgents:data.routing?.delegatedAgents||[],activityLogs,executionAudit:data.executionAudit,executionVerified:appliedIds.length?false:undefined}]); if(appliedIds.length)verifyLocalPersistence(key,messageId,appliedIds,data.actions);}catch(error){console.error('Direct employee chat failed:',error); appendRoomMessages(key,prev=>[...prev,{id:`direct-error-${Date.now()}`,sender:'agent',agentId:selectedAgent.id,agentName:selectedAgent.name,agentRole:selectedAgent.role,text:'目前無法連線到 AI 員工。請確認 Render Backend 與 Gemini API 是否正常。',timestamp:new Date().toLocaleTimeString('zh-TW',{hour:'2-digit',minute:'2-digit',hour12:false})}]);}finally{setDirectLoading(false);}};
 const handleSubmit=(e:React.FormEvent)=>{e.preventDefault();if(!inputText.trim()||busy)return;const text=inputText.trim();setInputText('');if(/\\b(安排|排程|排定|規劃).{0,20}(今天|明天|時間|時段)/i.test(text)&&selectedAgent.id==='manager')onSendMessage(text);else handleDirectSend(text);}; const handleCopy=(id:string,text:string)=>{navigator.clipboard.writeText(text);setCopiedId(id);setTimeout(()=>setCopiedId(null),2000);};
 return <div className="flex flex-col h-full w-full px-2 sm:px-4 py-3"><div className="mb-3 px-4 py-3 bg-white rounded-xl border border-[#E5E2DC] shadow-xs shrink-0"><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-2"><div className="p-1.5 rounded-lg bg-[#E8EFEB] text-[#385244]">{selectedAgent.icon}</div><div><div className="text-xs font-bold text-[#2D322E]">{selectedAgent.name} 聊天室</div><p className="text-[11px] text-[#6B726C]">同一聊天框內的 Manager 與專業 Agent 共用對話脈絡；切換聊天框才會隔離。</p></div></div><div className="flex flex-wrap gap-1.5">{selectableAgents.map(agent=><button key={agent.id} onClick={()=>{setDirectAgentId(agent.id);setSelectedTaskIds([]);}} className={`inline-flex items-center gap-1.5 px-2.5 py-1.5 rounded-lg border text-[11px] ${directAgentId===agent.id?'bg-[#385244] text-white border-[#385244]':'bg-white text-[#4A504B] border-[#DDD8CE] hover:bg-[#F4F0E8]'}`}>{agent.icon}<span>{agent.name}</span></button>)}</div></div></div><div className="flex-1 overflow-y-auto custom-scrollbar pr-1"><ChatTaskChecklist workTasks={workTasks} studyTasks={studyTasks} selectedTaskIds={selectedTaskIds} onSelectionChange={setSelectedTaskIds} onToggleWorkTask={onToggleWorkTask} onToggleStudyTask={onToggleStudyTask}/><div className="space-y-4">{displayMessages.map(msg=>{const isUser=msg.sender==='user';const isManager=msg.sender==='manager';const open=expandedActivities[msg.id]??false;return <div key={msg.id} className={`flex flex-col ${isUser?'items-end':'items-start'}`}><div className={`max-w-[95%] sm:max-w-[88%] rounded-2xl p-4 sm:p-5 shadow-xs ${isUser?'bg-[#385244] text-white rounded-tr-none':'bg-white border border-[#E5E2DC] text-[#2D322E]'}`}><div className={`flex items-center justify-between gap-2 pb-2 mb-2 border-b ${isUser?'border-[#4E6B56]':'border-[#EBE8E1]'}`}><div className="flex items-center gap-2"><div className={`w-7 h-7 rounded-lg flex items-center justify-center ${isUser?'bg-[#2B4035] text-white':'bg-[#E8EFEB] text-[#385244]'}`}>{isUser?<User className="w-4 h-4"/>:<Bot className="w-4 h-4"/>}</div><div><div className={`text-xs font-bold ${isUser?'text-white':'text-[#2D322E]'}`}>{isUser?'你':`${msg.agentName||(isManager?'Manager':'AI 員工')}${msg.agentRole?` · ${msg.agentRole}`:''}`}</div><span className={`text-[10px] font-mono ${isUser?'text-[#DCE7DF]':'text-[#8C938D]'}`}>{msg.timestamp}</span></div></div>{!isUser&&<button onClick={()=>handleCopy(msg.id,msg.text)} className="p-1 rounded text-[#8C938D] hover:bg-[#F4F0E8]">{copiedId===msg.id?<Check className="w-3.5 h-3.5"/>:<Copy className="w-3.5 h-3.5"/>}</button>}</div>{!isUser&&msg.activityLogs?.length?<div className="mb-3 rounded-xl bg-[#F8F7F4] border border-[#E5E2DC] overflow-hidden"><button onClick={()=>setExpandedActivities(p=>({...p,[msg.id]:!p[msg.id]}))} className="w-full flex items-center justify-between px-3 py-2 text-xs text-[#4A504B]"><span>Agent 協作執行流程（{msg.activityLogs.length} 步驟）</span><span className="flex items-center gap-1">{msg.executionVerified===true?<><ShieldCheck className="w-3.5 h-3.5"/>已驗證</>:msg.executionVerified===false&&msg.activityLogs.some(log=>log.action==='套用 Task 更新')?<><AlertTriangle className="w-3.5 h-3.5"/>驗證中</>:null}{open?'收起':'展開'}</span></button>{open&&<div className="p-3 border-t border-[#E5E2DC]"><AgentActivityPipeline logs={msg.activityLogs} compact/></div></div>:null}<div className={`prose prose-sm max-w-none leading-relaxed break-words ${isUser?'text-white':'text-[#2D322E]'}`}>{isUser?<p className="text-sm whitespace-pre-wrap">{msg.text}</p>:<ReactMarkdown>{msg.text}</ReactMarkdown>}</div>{!isUser&&msg.executionAudit?<div className="mt-3 px-3 py-2 rounded-lg bg-[#F8F7F4] border border-[#E5E2DC] text-[10px] text-[#6B726C]"><div className="font-semibold text-[#4A504B]">執行稽核</div><div className="mt-0.5">{auditSummary(msg.executionAudit)}</div></div>:null}{isManager&&msg.proposedTimeBlocks?.length?<button onClick={()=>onApplyScheduleToToday(msg.proposedTimeBlocks!)} className="mt-3 px-3 py-1.5 rounded-lg bg-[#385244] text-white text-xs">套用時間安排</button>:null}</div></div>})}{busy&&<div className="p-4 rounded-2xl bg-white border border-[#BCD2C3] shadow-sm text-xs text-[#4A504B]"><Sparkles className="inline w-4 h-4 mr-2 animate-pulse"/>{selectedAgent.name} 正在理解你的意思…</div>}<div ref={messagesEndRef}/></div></div><form onSubmit={handleSubmit} className="mt-3 flex gap-2"><input value={inputText} onChange={e=>setInputText(e.target.value)} disabled={busy} placeholder={`跟 ${selectedAgent.name} 說點什麼…`} className="flex-1 px-4 py-3 rounded-xl border border-[#DDD8CE] bg-white text-sm outline-none focus:ring-2 focus:ring-[#BCD2C3]"/><button type="submit" disabled={busy||!inputText.trim()} className="px-4 py-3 rounded-xl bg-[#385244] text-white disabled:opacity-50"><Send className="w-4 h-4"/></button></form></div>;
};